#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> p(n);
  iota(p.begin(), p.end(), 0);
  do {
    // Do something
  } while (next_permutation(p.begin(), p.end()));
  return 0;
}